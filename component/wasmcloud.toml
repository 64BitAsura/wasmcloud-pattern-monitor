name = "Pattern Monitor Component"
language = "rust"
type = "component"

[component]
wasm_target = "wasm32-wasip2"
wit_world = "pattern-monitor"
build_command = "cargo build --release --target wasm32-wasip2"
build_artifact = "target/wasm32-wasip2/release/pattern_monitor.wasm"
